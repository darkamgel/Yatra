import 'package:flutter/material.dart';


class EarningsTabPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      child: Center(
        child:Text("THis is earning page"),
      ),
    );
  }
}
